# Machine-local Fish variables
#
# Install a private copy from the repository root:
#   install -m 600 fish/.config/fish/secrets.fish.example ~/.config/fish/secrets.fish
#
# Replace placeholders only in the private copy. Keep real values out of Git
# and the Nix store.
#
# set -gx SERVICE_API_KEY replace-with-local-value

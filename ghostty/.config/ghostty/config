#command = zsh --login -c 'tmux attach -t "mux" || tmux new -s "mux"'
command = $(which zsh) --login

window-width = 132
window-height = 48

theme = GruvboxDark
#font-family = Fira Code
font-family = FiraCode Nerd Font Mono
font-size = 16

#cursor-style = block
#cursor-style-blink = false

#keybind = ctrl+shift+n=
